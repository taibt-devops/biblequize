/**
 * User identity card just under the sidebar header on desktop.
 * Avatar background uses tier color from data/tiers.ts; the dropdown
 * menu is the shared {@link UserDropdown} component so the same 5
 * items show whether the user clicks here or the mobile top bar.
 *
 * Task 1 scaffold: empty placeholder. Task 2 wires avatar + name +
 * tier subtitle + UserDropdown.
 */
export default function SidebarUserCard() {
  return null
}
