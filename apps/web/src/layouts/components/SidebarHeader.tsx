/**
 * Top of the desktop sidebar — Logo "Bible Quiz" gold (left) and the
 * NotificationBell (right). Replaces the previous fixed top bar's
 * logo + bell slot now that the top bar is hidden on desktop (Hướng
 * B). Hidden on mobile via the parent {@code AppLayout} which only
 * renders the sidebar at md+.
 *
 * Task 1 scaffold: empty placeholder. Task 2 wires logo + bell.
 */
export default function SidebarHeader() {
  return null
}
