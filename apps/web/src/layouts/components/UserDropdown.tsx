/**
 * Avatar button with click-to-open menu. Items match the current
 * AppLayout fixed header (5 items: Profile / Achievements / Help /
 * Language toggle / Logout). Mounted by SidebarUserCard (desktop)
 * and MobileTopBar (mobile).
 *
 * Task 1 scaffold: empty placeholder. Task 2 wires the dropdown +
 * click-outside handler.
 */
export default function UserDropdown() {
  return null
}
