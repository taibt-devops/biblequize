/**
 * Reusable notification bell with dropdown panel. Mounted by both
 * SidebarHeader (desktop) and MobileTopBar (mobile) so the
 * notification UX is consistent across viewports.
 *
 * Logic extracted from the orphan {@code components/Header.tsx} in
 * Task 2 of the AppLayout Hướng B refactor — see
 * docs/prompts/PROMPT_LAYOUT_HUONG_B.md.
 *
 * Task 1 scaffold: empty placeholder. Task 2 fills in logic +
 * dropdown panel.
 */
export default function NotificationBell() {
  return null
}
