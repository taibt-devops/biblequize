/**
 * Sticky top bar visible only on mobile (md:hidden). Composes Logo +
 * {@link NotificationBell} + {@link UserDropdown}. Required because
 * the desktop sidebar — which carries the same identity surfaces — is
 * hidden on mobile (AppLayout renders sidebar with hidden md:flex).
 *
 * Task 1 scaffold: empty placeholder. Task 3 wires layout.
 */
export default function MobileTopBar() {
  return null
}
