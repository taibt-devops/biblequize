/**
 * 4-tab bottom nav visible only on mobile (md:hidden). Currently
 * inlined in {@code AppLayout.tsx}; Task 3 of the Hướng B refactor
 * extracts it to this component so AppLayout stays under 300 LOC.
 *
 * Tabs: Trang chủ / Xếp hạng / Nhóm / Cá nhân (matches the existing
 * navItems list — see HM-MB-2 in BUG_REPORT_HOME_POST_IMPL.md for the
 * 320px label-wrap test that runs after this lands).
 *
 * Task 1 scaffold: empty placeholder. Task 3 fills in the tab list.
 */
export default function MobileBottomTabs() {
  return null
}
