# 2026-09-15 — Học Thuộc: nạp Bản Truyền Thống 1926 và bật tính năng trên prod

> **Source**: user chọn tạm dùng BTT 1926 (public domain) để lối vào Học Thuộc hiện lên — DECISIONS 2026-09-15. · **Scope**: đổi bản dịch đang dùng + seed toàn văn + import prod. Plan gốc: `docs/todo/active/2026-09-15-hoc-thuoc-cau-goc.md` (HT-5 được thay bằng BT-3).
> **Branch/worktree**: `feat/memorize-btt1926` @ `../biblequize-btt1926`

## Nguồn dữ liệu

- eBible.org `vie1934` — "Kinh Thánh Vietnamese Bible", dịch giả William Cadman (1883–1948), Tân Ước 1923, Cựu Ước 1925 (trọn bộ 1926). **Public Domain**.
- Tải `https://ebible.org/Scriptures/vie1934_usfm.zip`: 66 file USFM, chỉ các marker `\id \ide \h \toc1 \toc2 \mt1 \mt2 \c \p \v` — **31.102 câu / 1.189 chương** (khớp canon 66 sách Tin Lành, không Thứ Kinh).

## Tasks

- BT-1 Quyết định + spec + BACKLOG + task
  - Status: [x] DONE · Files: `DECISIONS.md`, `docs/spec/SPEC_USER_v3.1.md` §5.1.1, `docs/spec/BACKLOG.md` BL-1, file này, `TODO.md` · Test: spec-audit không NEW broken
  - **Spec impact**: [x] SPEC_USER §5.1.1 · [x] BL-1 · **Spec strategy**: [x] (a) update inline

- BT-2 `BibleVerse.ACTIVE_VERSION = "BTT1926"` thay cho `BTTHD_2011` trong code chạy thật
  - Status: [x] DONE · Files: `BibleVerse` (+ `BTT_1926`, `ACTIVE_VERSION`), `BiblePassageService`, `MemoryVerseService`, `BibleStatusController`, `BibleTextImporter` (+ pattern mặc định `seed/bible/btt1926/*.json`), `application.yml`, tests (fixture BE+FE đổi sang `BTT1926`, + test khoá `ACTIVE_VERSION`) · Test: BE Tầng 3 955 run / 3 fail có sẵn; Vitest vùng ảnh hưởng 57/57
  - **Spec strategy**: [x] (c) (spec ở BT-1)

- BT-3a (fix-on-touch) Sửa bảng số câu sai trong `BibleStructure.java` + `bibleData.ts`
  - Status: [x] DONE · Phát hiện khi `BibleSeedContentTest` so seed với `BibleStructure`: 4 sách sai (tổng 31.152 thay vì 31.102) — **Lê-vi Ký 19–27** (vd ch.19: 24 → 37), **Truyền Đạo 4–5** (đánh số kiểu Hê-bơ-rơ), **Ê-sai 34–66** (vd ch.53: 2 → 12, ch.40: 13 → 31), **Ê-phê-sô 4–6** (ch.5: 20 → 33). Ảnh hưởng thật: bộ chọn chương/câu Luyện Tập + Học Thuộc (không chọn được Ê-sai 53:5), `SessionService.validateRange`, `/api/books/{name}/structure`. Sửa theo seed chuẩn; 0 câu hỏi seed nằm ngoài phạm vi mới · Test: `BibleStructureTest` +1, `bibleData.test.ts` (mới) 2/2. BE 979 run / 3 fail có sẵn; Vitest 1484 pass
  - **Spec strategy**: [x] (c) [no-spec-impact] (sửa dữ liệu tham chiếu cho đúng chuẩn, không đổi hành vi được spec mô tả)

- BT-3 Script chuyển USFM → seed JSON + 66 file seed + test toàn vẹn dữ liệu
  - Status: [x] DONE · Files: `scripts/bible/usfm_to_seed.py` (từ chối marker USFM lạ, NFC, 1 câu/dòng), `apps/api/src/main/resources/seed/bible/btt1926/NN-Book.json` (66 file, 31.102 câu, 6,1 MB), `BibleSeedContentTest` 4/4 (đủ 66 sách đúng thứ tự/tên file importer, mọi chương đủ câu 1..n theo `BibleStructure`, tổng 31.102, chữ sạch NFC không markup, soát Giăng 3:16 / Sáng Thế 1:1 / Thi Thiên 23:1). Tái tạo: tải `vie1934_usfm.zip` → `python scripts/bible/usfm_to_seed.py <usfm_dir> apps/api/src/main/resources/seed/bible/btt1926`
  - **Spec strategy**: [x] (c)

- BT-4 Import local end-to-end
  - Status: [x] DONE · MySQL 8 tạm, `ddl-auto: none`, `BIBLE_IMPORT_ENABLED=true`: import **31.102 câu / 66 sách trong ~2 phút 15 giây** (chạy sau khi app đã phục vụ request); status → `available:true`; khởi động lại với cờ vẫn bật → `0 verses written` (~1 giây, idempotent). API: Ê-sai 53:5 trả đúng UTF-8, thêm Ê-sai 53:4–6 → 201 (xác nhận BT-3a). Trình duyệt thật (config Playwright riêng, không global teardown): thẻ hiện ở `/practice` → thêm Giăng 3:16 (preview chữ thật) → ngữ cảnh 3:14–18 → sắp xếp cụm đạt → L1, `due=false`; lặp 3/3. Mobile 390px: Practice, chọn câu (Thi Thiên 23:1–3), danh sách, bài sắp xếp — không cuộn ngang

- BT-4a (bug phát hiện ở BT-4) `next_review_at` bị MySQL làm tròn lên giây kế
  - Status: [x] DONE · Nguyên nhân: `TIMESTAMP` không lưu phần lẻ giây và **làm tròn half-up**; câu mới thêm với `now` có nano ≥ .5 → lưu thành giây kế → `countDue`/`findDue` (`<= now`) bỏ sót trong ~0,5 giây ngay sau khi thêm → nút "Ôn câu đến hạn" bị khoá tới lần refetch sau (staleTime 5 phút) dù item hiện "Đến hạn ôn". Sửa: `MemoryVerseService.storable()` cắt về giây cho `add` và `review` · Test: `MemoryVerseServiceTest` +2 (10/10); BE 980 run / 3 fail có sẵn; trình duyệt thật thêm → nút ôn bật ngay, lặp 3/3
  - **Spec strategy**: [x] (c) [no-spec-impact] (sửa lỗi, hành vi đúng như spec)

- BT-5 Merge `main` → push → deploy → import one-shot trên prod → verify
  - Status: [x] DONE · `main` fast-forward → `52167af7`, push. Image build từ worktree sạch: BE `sha256:1b42f406…`, FE `sha256:4016b5ae…` (rollback: BE `b86b21e7…`, FE `3d280819…`). Prod: sao lưu `/opt/biblequiz/compose.yml.bak-20260915-bible` → thêm `BIBLE_IMPORT_ENABLED: "true"` → pull + recreate api/web → log `[bible-import] done, 31102 verses written` (~3,5 phút) → DB `BTT1926 | 31102 | 66`, Giăng 3:16 + Ê-sai 53:5 đúng chữ UTF-8 → `GET https://forbible.org/api/public/bible/status` = `available:true`, health 200 → đặt lại cờ `"false"` trong compose.yml (không recreate: container đang chạy giữ cờ true nhưng importer idempotent, lần deploy sau đọc false). Trình duyệt thật trên prod (khách): `/practice` hiện thẻ "Học Thuộc câu gốc" + "Đăng nhập →" 
